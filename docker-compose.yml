services:
  app:
    build: .
    container_name: holzlinge-inventar
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:////data/holzlinge.db
      ADMIN_USER: ${ADMIN_USER:-admin}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-}
      SESSION_IDLE_HOURS: ${SESSION_IDLE_HOURS:-12}
      SHOPIFY_STORE: ${SHOPIFY_STORE:-}
      SHOPIFY_CLIENT_ID: ${SHOPIFY_CLIENT_ID:-}
      SHOPIFY_CLIENT_SECRET: ${SHOPIFY_CLIENT_SECRET:-}
      SHOPIFY_ADMIN_TOKEN: ${SHOPIFY_ADMIN_TOKEN:-}
      SHOPIFY_API_VERSION: ${SHOPIFY_API_VERSION:-2026-01}
      GEMINI_API_KEY: ${GEMINI_API_KEY:-}
      GEMINI_MODEL: ${GEMINI_MODEL:-gemini-3.1-flash-lite}
      IMAP_HOST: ${IMAP_HOST:-}
      IMAP_PORT: ${IMAP_PORT:-993}
      IMAP_USER: ${IMAP_USER:-}
      IMAP_PASSWORD: ${IMAP_PASSWORD:-}
      IMAP_FOLDER: ${IMAP_FOLDER:-INBOX}
      IMAP_PROCESSED_FOLDER: ${IMAP_PROCESSED_FOLDER:-verarbeitet}
      IMAP_POLL_HOURS: ${IMAP_POLL_HOURS:-6}
    volumes:
      - holzlinge_data:/data
    restart: unless-stopped

volumes:
  holzlinge_data:
